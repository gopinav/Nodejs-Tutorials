const add = (a, b) => {
  return a + b;
};

const subtract = (a, b) => {
  return a - b;
};

module.exports = {
  add,
  subtract,
};

// module.exports = (a, b) => {
//   return a + b;
// };

// exports = {
//   add,
//   subtract,
// };
// module.exports = add;
